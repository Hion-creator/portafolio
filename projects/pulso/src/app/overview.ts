import { Component, computed, inject } from '@angular/core';
import { RouterLink } from '@angular/router';
import { TicketStore } from './store';
@Component({standalone:true,imports:[RouterLink],template:`<section class="page-intro"><div><p class="eyebrow">UNA MIRADA A TU EQUIPO</p><h1>El panorama, claro<span>.</span></h1><p class="subtitle">Métricas calculadas a partir de las conversaciones de esta demo.</p></div><a routerLink="/bandeja" class="secondary">Volver a la bandeja ↗</a></section><section class="overview-grid"><article class="overview-card"><p class="eyebrow">RESOLUCIÓN</p><div class="ring" [style.background]="'conic-gradient(#446b52 '+rate()+'%, #eeeee8 0)' "><div><b>{{rate()}}%</b><span>resueltos</span></div></div><p class="text-center text-stone-500">{{resolved()}} de {{store.tickets().length}} conversaciones</p></article><article class="overview-card"><p class="eyebrow">DISTRIBUCIÓN POR CATEGORÍA</p><h2>¿En qué necesitan ayuda?</h2>@for(c of categories();track c.name){<div class="mt-6"><div class="flex justify-between text-sm mb-2"><span>{{c.name}}</span><b>{{c.count}}</b></div><div class="bar"><i [style.width.%]="c.count/store.tickets().length*100"></i></div></div>}</article><article class="overview-card"><p class="eyebrow">DETRÁS DEL PROYECTO</p><h2>Una interfaz con intención.</h2><p class="text-stone-600 leading-7 mt-4">Pulso explora cómo integrar asistencia de IA en una tarea real, manteniendo a la persona al mando.</p><ul class="tech-list"><li><b>Angular</b><span>Signals, rutas diferidas y formularios reactivos.</span></li><li><b>Tailwind CSS</b><span>Diseño adaptable, componentes y estados accesibles.</span></li><li><b>Python + Ollama</b><span>API tipada, salida estructurada y revisión humana.</span></li></ul><p class="text-xs text-stone-500 mt-6">Demo de portafolio · Sin envío de mensajes ni conexión a clientes reales.</p></article></section>`})
export class Overview {store=inject(TicketStore);resolved=computed(()=>this.store.tickets().filter(t=>t.status==='Resuelto').length);rate=computed(()=>Math.round(this.resolved()/Math.max(1,this.store.tickets().length)*100));categories=computed(()=>Array.from(new Set(this.store.tickets().map(t=>t.category))).map(name=>({name,count:this.store.tickets().filter(t=>t.category===name).length})));}
